# Production environment template for backend deployment
# Copy this file to .env.production (for your backend host) and set real values.

# Web server
PORT=3000
NODE_ENV=production
SESSION_SECRET=change_this_secret
CORS_ORIGINS=*

# MongoDB
MONGODB_URI=mongodb+srv://user:password@cluster0.mongodb.net/trackwise?retryWrites=true&w=majority

# Email (optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# AI (optional)
OPENAI_API_KEY=
